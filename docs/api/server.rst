server
======

Entry point module. Registers all MCP tools on the ``mcp`` application instance
and exposes a ``main()`` function for stdio transport.

.. autofunction:: google_scholar_search_mcp.server.main
